# Additional requirements for Web Research Agents
# Add these to your existing requirements.txt

# Web scraping and parsing
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# RSS feed parsing
feedparser>=6.0.0

# HTML parsing and extraction
newspaper3k>=0.2.8

# Optional: For more advanced scraping (if needed)
# scrapy>=2.6.0
# selenium>=4.5.0

# Data processing
python-dateutil>=2.8.0

# Async support (already in base requirements, but needed for agents)
# aiohttp>=3.8.0

# JSON handling (built-in, but ensuring compatibility)
# ujson>=5.5.0